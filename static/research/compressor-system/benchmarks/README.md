# Benchmark records

Store raw result CSV/JSON, environment manifests, codec versions, commands, hashes,
and plots generated from those records. Do not add hand-entered or illustrative
performance curves.

